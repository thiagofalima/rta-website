
import styled from "styled-components"

const HeaderWrapper = styled.div`
    width: 100vw;
    color: white;
    padding: 16px;
    background-color: blue;
`

export const Header = () => {
    return (
        <HeaderWrapper>
            Hello
        </HeaderWrapper>
    )
}